# Frontend (web/.env.local). Copy to web/.env.local and fill in.

# The FastAPI backend's URL. Read server-side only (route handlers), never
# exposed to the browser.
BACKEND_URL=http://localhost:8090
