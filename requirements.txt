# Floors verified together on 2026-08-19: a clean `pip install -r requirements.txt`
# at these versions passed the full test suite (304 tests) and a live
# /classify + /report round trip. Not exact-pinned — this is an application's
# direct dependencies, not a lockfile.

# Core
anthropic>=0.111.0

# RAG
chromadb>=1.5.9
sentence-transformers>=5.6.0

# Reports
reportlab>=5.0.0

# API (the Next.js frontend in web/ is the only UI)
fastapi>=0.141.1
uvicorn[standard]>=0.49.0
pydantic>=2.13.4

# Ingestion / utils
pypdf>=6.13.3
requests>=2.34.2
python-dotenv>=1.2.2
pytest>=9.1.1
# Decision-path coverage of rules.py: eval/coverage_matrix.py --branches
coverage>=7.15.3
httpx>=0.28.1           # FastAPI TestClient
